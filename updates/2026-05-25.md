# updates/2026-05-25.md

## What Was Done

- Applied `setup.md` documentation scaffold at repo root (`AGENTS.md`, `CHANGELOG.md`, `PROJECT_CONTEXT.md`, `updates/`).
- Fixed trigonometry PDF mislabeled as triangles/circles in topic extraction (subtopics-first chapter inference, trig density override).
- RAG slot 5 regeneration response written for Class 10 Circles HOTS fusion item.

## What Was Learned

- NCERT chapter PDFs can index mixed exercise types; filename and subtopic lists are more reliable than scoring the full chunk blob alone.
- Generate UI chapter label comes from live `topic-profile` API — stale uvicorn or cached selection shows old labels.

## What Failed

- Earlier `topic_extractor.py` edit failed on whitespace mismatch; resolved by matching file’s spaced formatting.
- `pytest` not installed in default Python environment on this machine — tests run via direct imports instead.

## What's Next

- Restart backend and re-select documents to confirm UI shows correct `locked_chapter`.
- Commit documentation scaffold when the user wants it in git.
- Continue quality regeneration if backend flags more empty or duplicate stems.
